import React, {FC} from 'react';
import CategoryModel from "../../../models/CategoryModel";

interface MenuSelectProps {
    categorysList: CategoryModel[];
}

const MenuSelect: FC<MenuSelectProps> = ({categorysList}) => {
    return (<div></div>);
}

export default MenuSelect;